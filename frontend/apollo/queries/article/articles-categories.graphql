query Category($id: ID!){
  category(id: $id) {
    name
    articles {
      id
      title
      content
      image {
        url
      }
      published
      category {
        id
        name
      }
    }
  }
}
